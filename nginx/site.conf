server {
    listen 80;
    server_name asomedomainfortestingsmth.cloud;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name asomedomainfortestingsmth.cloud;

    ssl_certificate /etc/letsencrypt/live/asomedomainfortestingsmth.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/asomedomainfortestingsmth.cloud/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # IP whitelist: allow only listed addresses/subnets.
        # Replace examples below with your real external IPs.
        allow 127.0.0.1;
        allow ::1;
        # allow 203.0.113.10;
        # allow 198.51.100.0/24;
        allow 5.77.196.171;
        deny all;

        proxy_pass http://hedging:8081/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
